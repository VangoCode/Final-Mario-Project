void setupController() { // setup the controller connection to the serial for arduino
  //String portName = Serial.list()[1]; //change the 0 to a 1 or 2 etc. to match your port
  //myPort = new Serial(this, portName, 9600);
  
}


void getControllerData() {
  //if ( myPort.available() > 0)  // get the data
    //{  // If data is available,
    //  val = myPort.readStringUntil('\n');         // read it and store it in val
    //  val = trim(val); // trim excess spaces in val
    //  if (val!= null) { // if it's not null


    //    valArray = split(val, ' '); // split each of the four words into a separate array
    //    for (int i = 0; i < 4; i++) { // score the first character of each word into a char array which we then use to compare
    //      valArray[i] = valArray[i].substring(0, 1); 
    //      valChar[i] = valArray[i].charAt(0);
    //      //          println(valArray[i]);
    //    }
    //  }
    //}
}
